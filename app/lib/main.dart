// Default entry point (used by `flutter run` without `-t`). Runs the dev flavour.
// Production builds must use `-t lib/main_prod.dart --flavor prod`.
import 'main_dev.dart' as dev;

void main() => dev.main();
